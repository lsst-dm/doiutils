<!-- Delete the sections that do not apply -->

### Bug fixes

- Authors with no given name, such as the organizational author used for builder papers, no longer have an empty first name in the DOI record.
  ELink stores an empty first name as a null value, so ``update-paper-info`` reported a spurious author change and resubmitted the record on every invocation.
